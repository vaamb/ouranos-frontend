// eslint-disable
// Not used at runtime: this file only exists so that PyCharm resolves the `$lib`
// alias in imports. The real alias comes from SvelteKit / `tsconfig.json`.
System.config({
    "paths": {
        "$lib/*": "./src/lib/*",
    }
});
